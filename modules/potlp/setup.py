from setuptools import setup, find_packages

setup(name='potlp',
      version='0.1.0',
      description='Core code for PO-TLP.',
      license="MIT",
      author='Gregory J. Stein',
      author_email='gjstein@gmu.edu',
      packages=find_packages(),
      install_requires=[])
